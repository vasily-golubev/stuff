\section{—татическа€ одномерна€ задача упругости}

\subsection{—ильна€ постановка (strong form)}
–ассмотрим задачу об одномерном деформировании упругого стержн€ с переменным сечением $A(x)$ под действием не завис€щей от времени силы $f(x)$, произвольно распределЄнной вдоль него.
ѕусть стержень ориентирован вдоль оси $OX$, и его концы имеют координаты $x=0$ и $x=L$.
“огда в состо€нии равновеси€ справедливо уравнение
\begin{equation}
    \label{eq:strong_form}
    \begin{gathered}
    \frac{d}{dx}(A(x)E(x)\frac{du}{dx}) + f = 0, \\
    \sigma(x)=E(x)\frac{du}{dx}.
    \end{gathered}
\end{equation}
где $E(x)$~--- модуль ёнга дл€ вещества $[\frac{N}{m^2}]$, из которого сделан стержень, $A(x)$~--- площадь сещени€ стержн€ $[m^2]$, $u(x)$~--- смещение точки стержн€ из-за его раст€жени€ или сжати€ $[m]$, $\sigma(x)$~--- напр€жение в стержне $[\frac{N}{m^2}]$, $f(x)$~--- внешн€€ сила, действующа€ на элемент стержн€ $[\frac{N}{m}]$.

\subsection{—лаба€ постановка (weak form)}

¬ общем случае, может быть записано следующее интегральное уравнение, называющеес€ "слабой формой"\ (weak form). Ќеобходимо найти функцию $u(x) \in S\{u|u(0)=u_0\}$, такую что $\forall \omega \in V\{v|v(0)=0\}$ справедливо равенство
\begin{equation}
    \label{eq:weak_form}
    \int_0^L \frac{d \omega}{dx}EA\frac{du}{dx}dx=\int_0^L \omega fdx + \omega(L)tA(L),
\end{equation}
где $t=\sigma(L)$~--- напр€жение на правом конце стержн€. ‘ункции $\omega$ называютс€ \textit{весовыми функци€ми} (weighting functions).

\clearpage
